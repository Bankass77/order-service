services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=orderservice'
      - 'POSTGRES_PASSWORD=Homeboarding1979!'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
    volumes:
      - db:/var/lib/postgresql/data
volumes:
  db:
    driver: local
